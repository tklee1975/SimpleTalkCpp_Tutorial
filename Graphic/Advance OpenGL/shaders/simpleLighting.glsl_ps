#version 330

layout(location = 0) out vec4 outColor;

in vec4 p_color;
in vec3 p_normal;
in vec3 p_lightDir;

void main() {
   //outColor = p_normal;
   
   float d = dot(p_lightDir, p_normal);
   outColor = vec4(d,d,d,1);
}